enum MenuState { home, favourite, message, profile }
enum StaffMenuState { home, staff, admin, volunteer_report, trip_report , profile }
enum AdminMenuState { home, manage_application, organize_trips}